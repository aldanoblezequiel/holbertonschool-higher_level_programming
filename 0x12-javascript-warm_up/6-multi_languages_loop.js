#!/usr/bin/node
const array = ['C is fun', 'Python is cool', 'JavaScript is amazing'];
for (let index = 0; index < array.length; index++) {
  const argument = array[index];
  console.log(argument);
}
