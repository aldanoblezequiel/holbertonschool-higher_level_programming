#!/usr/bin/python3

"""
Ckecks if same instance of class
"""


def is_same_class(obj, a_class):
    """ckecks if are the same calses"""
    if type(obj) == a_class:
        return True
    else:
        return False
